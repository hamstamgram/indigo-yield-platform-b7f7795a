/**
 * Legacy shadcn Toaster - no longer used.
 * All toasts route through Sonner via the useToast shim.
 * This component is kept for import compatibility but renders nothing.
 */
export function Toaster() {
  return null;
}
