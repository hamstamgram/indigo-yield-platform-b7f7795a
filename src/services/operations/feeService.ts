// Minimal fee service placeholder; align later with actual fee RPC outputs
export const feeService = {
  async applyFees() {
    return { success: false, message: "Fee service not implemented." };
  },
};
