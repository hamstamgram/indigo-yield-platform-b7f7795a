// Portfolio API was using legacy positions table; leave a stub to avoid type errors until migrated to investor_positions.
export const portfolioApi = {
  async getUserPortfolios() {
    return [];
  },
};
