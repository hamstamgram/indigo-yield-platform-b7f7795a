/**
 * Admin Transaction Routes
 * Redirects to consolidated Ledger page
 */

import { Route, Navigate } from "react-router-dom";

export function TransactionRoutes() {
  return <>{/* All transaction routes now handled by LedgerRoutes */}</>;
}
