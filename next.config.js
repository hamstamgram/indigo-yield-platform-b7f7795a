/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  // You may add other Next.js configurations here if needed
};

module.exports = nextConfig;
