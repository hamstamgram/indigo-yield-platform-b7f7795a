name: Staging Validation

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  PNPM_VERSION: '8'
  ENVIRONMENT: 'staging'

# NOTE: Web deployment is handled by Lovable.dev, not this workflow.
# This workflow validates the build and runs database migrations.

jobs:
  validate:
    name: Validate Build
    runs-on: ubuntu-latest
    environment:
      name: staging

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run database migrations
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_STAGING_DB_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_STAGING_SERVICE_ROLE_KEY }}
        run: |
          npx supabase db push --db-url "$SUPABASE_DB_URL" || echo "Using direct psql"
          # Fallback to direct psql if supabase CLI fails
          for migration in supabase/migrations/*.sql; do
            echo "Applying migration: $migration"
            psql "$SUPABASE_DB_URL" -f "$migration" || exit 1
          done

      - name: Build application
        env:
          VITE_SUPABASE_URL: ${{ secrets.STAGING_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.STAGING_SUPABASE_ANON_KEY }}
          VITE_SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          VITE_POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          VITE_APP_VERSION: ${{ github.sha }}
          NODE_ENV: production
        run: pnpm build

      - name: Create Sentry Release
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
        run: |
          npm install -g @sentry/cli
          export SENTRY_RELEASE="${{ github.sha }}"

          # Create release
          sentry-cli releases new $SENTRY_RELEASE

          # Upload source maps
          sentry-cli releases files $SENTRY_RELEASE upload-sourcemaps ./dist \
            --url-prefix "~/" \
            --rewrite

          # Set commits
          sentry-cli releases set-commits $SENTRY_RELEASE --auto

          # Finalize release
          sentry-cli releases finalize $SENTRY_RELEASE

          # Mark as deployed
          sentry-cli releases deploys $SENTRY_RELEASE new -e staging

      - name: Notify validation status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const status = '${{ job.status }}' === 'success' ? '✅' : '❌';
            const sha = context.sha.substring(0, 7);

            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: '${{ job.status }}' === 'success' ? 'success' : 'failure',
              description: `Staging validation ${status}`,
              context: 'staging/validation'
            });
