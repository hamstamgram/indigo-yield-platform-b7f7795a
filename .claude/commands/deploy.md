---
description: Deploy indigo platform to Vercel
---
Help me deploy the indigo-yield-platform:

1. Run type check: `npm run type-check`
2. Run tests: `npm run test`
3. Build: `npm run build`
4. If all pass, guide through Vercel deployment

Check for any environment variables that need updating.

$ARGUMENTS
